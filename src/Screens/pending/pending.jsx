import React from 'react';
import { View , Text ,ScrollView } from 'react-native';

const Pending = () => {
    return (
        <ScrollView>
            <Text>Your application is on pending</Text>
        </ScrollView>
    )
}

export default Pending;
